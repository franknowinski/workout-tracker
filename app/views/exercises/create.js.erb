if ($('.' + $('#exercise_muscle_group_id').val() + '-workout').length == 0){
  $('#workout-plan-table').append("<%= j render 'table', exercise: @exercise %>");
} else {
  $('.<%= @exercise.muscle_group.name.downcase %>-group-row div.col-sm-8 tbody').append("<%= j render 'table_row', exercise: @exercise %>");
}

$('#exercise-form input.form-control').val('');
$('#exercise_muscle_group_id').val('')
