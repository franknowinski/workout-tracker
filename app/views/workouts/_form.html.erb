<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :sets %>
<%= f.number_field :sets, min: 1, max: 10 %>

<%= f.label :reps %>
<%= f.number_field :reps, min: 1, max: 30 %> <br><br>

<%= f.submit class: 'btn btn-primary' %>

<%= link_to 'Delete Workout', workout_plan_destroy_workout_path(@workout.exercise.workout_plan, @workout), method: :delete, class: 'btn btn-danger' %>
